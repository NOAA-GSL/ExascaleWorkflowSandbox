[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]


[tool.flake8.messages_control]
disable = [
    "--max-line-length",
]

[tool.flake8]
--max-line-length = 120

[flake8]
max-line-length = 100
